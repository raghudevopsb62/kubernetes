apiVersion: v1
kind: Pod
metadata:
  name: sample-resources
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
      requests:
        memory: "10Gi"
        cpu: "2000m"
      limits:
        memory: "10Gi"
        cpu: "2000m"
